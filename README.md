# 消防安全公共知识 · 刷题

基于 5 份《消防安全公共知识考试》模拟卷（卷A~E）的本地刷题小程序。

## ✨ v0.1 特性

- **210 题题库**：卷A~E × 42题（填空10 + 选择20 + 判断10 + 简答2），数据内嵌单文件
- **四种题型全支持**：
  - 选择题：点击选项即判分，错误标红/正确标绿并显示正确答案
  - 判断题：点 √ / × 判分
  - 填空题：手动输入，**严格匹配**判分（trim 后完全一致，如 "10" ≠ "10%"）
  - 简答题：查看参考答案折叠区 + **自评模式**（✅ 我答对了 / ❌ 我没答对，没答对自动进错题本）
- **三种刷题模式**：按卷筛选 / 按题型筛选 / 🎲 随机洗牌 / 📕 错题重刷
- **进度持久化**：localStorage 保存已刷/答对/正确率/错题本，刷新不丢；支持一键重置
- **响应式设计**：桌面宽屏 + 手机窄屏自适应（工具栏自动换行、无横向溢出）
- **零依赖零安装**：单文件 index.html，双击浏览器即用；可离线分享（只需发一个文件）

## 📁 文件结构

| 文件 | 说明 |
|---|---|
| `index.html` | 交付产物（单文件，数据内嵌，双击即用） |
| `questions.json` | 210 题结构化数据（可单独维护） |
| `parse_docs.py` | 从含答案 docx 解析题目 → questions.json |
| `build_html.py` | 构建 index.html（数据 + 前端代码合并） |

## 🔧 重新构建

```bash
# 更新题库后重建：
python parse_docs.py   # 从 卷X_含答案.docx 重新解析
python build_html.py   # 生成新 index.html
```

## 📈 版本计划

- v0.1 ✅ 功能完整版（当前）
- v0.2 🚧 视觉升级（液态玻璃 + Apple 设计语言，待确认设计方向）
